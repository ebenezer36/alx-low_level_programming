#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 * @j: number one.
 * @t: number two.
 * Return: Add of number one and number two.
 */
int add(int j, int t)
{
	return (j + t);
}
